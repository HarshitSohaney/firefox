[package]
name = "cast"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
prost = "0.12"
serde_json = "1.0"

[build-dependencies]
prost-build = "0.12"

[lib]
crate-type = ["staticlib"]
